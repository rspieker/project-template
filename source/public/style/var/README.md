#Here be .css files which need to be available before the base files (../base) in the `combined.css`
One can put all files containing variables here, these get processed by Myth(.io) _only_ for the combined.css
